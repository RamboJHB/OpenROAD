#pragma once

// The destination installs this header as <dpl2/DRCChecker.h>.  Repository-
// local tests compile directly from src/, so provide only the matching include
// layout here without changing the delivered header.
#include <drc/DRCChecker.h>
