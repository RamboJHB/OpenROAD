#pragma once

// Test-only UDM compatibility surface.  The production header supplies the
// netlist-change classes; the local repair chain only needs their declarations
// while parsing DePlace.h.
#include <fake_udm.h>
